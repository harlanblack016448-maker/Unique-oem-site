# 非展示优化与续作验收（2026-09-12）

本轮以用户当前版本 `7d4a8f8` 为基线。用户要求保留已调整的内容显示，因此只完成非展示项；不恢复上一轮已离开当前工作副本的页面改动，也不将上一轮测试结果作为本轮通过证据。未提交、未推送、未部署。

## 已完成

- **确认回执后才计为成功**：HTTP 成功且 JSON 明确包含 `success: true` 或 `success: "true"` 才清空表单、显示成功并记录转化。非 2xx、无效 JSON、空回执、业务失败、网络错误均保留填写内容。
- **15 秒提交超时**：覆盖响应头及响应体读取，终止等待后恢复提交按钮。保留当前邮件客户端兜底及其文案。
- **防重复提交**：在请求进行中忽略重复提交；切换语言时保持正确的“正在发送”状态。
- **发布脚本检查**：保留明确区分 `--check-only` 与 `--publish` 的流程；补齐 grep 回退时读取错误的处理，避免检查工具出错却继续。新增测试验证只检查不会改动文件内容、Git index 或 refs。
- **资源接入**：首页与联系页仅新增请求辅助脚本并将 form.js 升至 v12；没有改动正文、表单选项或页面布局。

## 显示保护

续作前保存文件摘要并与最终文件比较；首页、联系页剔除上述脚本接入差异后与当前 Git 基线逐字一致。其余页面、样式、词典、共享界面及图片均未修改。保留：

- `184b28b`：八电极宽幅热点图、桌面客服悬浮按钮位置及相应样式。
- `7d4a8f8`：第三个热点位于 `left:22%; top:29%`。

未运行 `sync-copy.cjs` 或 `responsive-images.py`，避免批量覆盖当前显示差异。

## 本轮验证

| 检查 | 结果 |
| --- | --- |
| Python 回归 | 24 项通过（原 23 项及发布检查测试） |
| Node 回归 | 14 项通过（回执、网络错误、两阶段超时、防重复、语言状态、失败保留） |
| 只读发布检查测试 | 干净、已暂存、目录不一致、编辑器标记四种状态，均无文件/index/refs 内容改动 |
| 浏览器本地成功路径 | 隔离回执成功后表单重置，成功状态出现，模拟转化计数为 1，v12 与辅助脚本加载正确 |
| 显示保护 | 文件摘要与 HTML 脚本差异核对通过 |
| JavaScript / Shell 语法 | 通过 |

异常回执、超时及重复提交使用本地自动化测试，不启动真实邮件客户端、不发送真实询盘。浏览器成功测试由仅监听本机的 preview-test.py 提供模拟回执并阻止第三方请求。未声称验证实际邮件投递、生产客服接口或广告后台接收。

本轮无需重做全部 54 种页面/语言/宽度组合；展示代码未改动，以差异保护验证保留用户当前显示。上一轮的多尺寸检查不计作当前版本的完整视觉验收。

## 按用户要求暂缓

以下项目会影响页面显示，未恢复或应用：样品/目录“数量尚未确定”选项、首单 MOQ 与年采购量展示调整、型号询盘入口、型号列表折叠、额外的翻译及无障碍标签、静态英文回退同步、响应式图片批量接入、隐私页面补写。

隐私保留期限、删除流程及未核实产品事实仍需单独确认；本轮未改动这些承诺。原图片来源清单不因本轮无改图而自动获得重新认证，用户恢复的热点图应在未来素材核对时单独追溯，当前按要求保留。

## 后续操作

内容副本与指定推送副本只同步本轮文件；不覆盖工作区其他内容。可通过父目录 `prepush.sh --check-only` 检查两份副本一致性。发布仍需独立执行并核验 GitHub、Vercel 和线上页面，当前不发布。
